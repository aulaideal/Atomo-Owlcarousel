{% extends '@nucleus/partials/particle.html.twig' %}

{% block particle %}
    {% assets in 'head' with { priority: 11 } %}
        {% if particle.enabled %}
            <link rel="stylesheet" href="{{ url('gantry-theme://OwlCarousel2/css/owl.carousel.min.css') }}" type="text/css"/>
        {% endif %}
    {% endassets -%}

    {% assets in particle.jslocation|default('footer') with { priority: 10 } %}
        {% if particle.enabled %}
            {% do gantry.load('jquery') %}
            <script src="{{ url('gantry-theme://OwlCarousel2/js/owl.carousel.min.js') }}" type="text/javascript"></script>
        {% endif %}
    {% endassets -%}
{% endblock %}